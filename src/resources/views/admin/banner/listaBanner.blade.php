<div class="d-flex justify-content-end mb-3"><a class="btn btn-primary" href="{{ route('admin.banner.create') }}">Novo banner</a></div>
<div class="card shadow-sm"><div class="card-header d-flex justify-content-between"><h3 class="card-title mb-0">Banners cadastrados</h3><input class="form-control form-control-sm w-auto" placeholder="Pesquisar banners"></div><div class="table-responsive"><table class="table table-hover align-middle m-0"><thead><tr><th>ID</th><th>Imagem</th><th>Título</th><th>Status</th><th class="text-end">Ações</th></tr></thead><tbody>@forelse($banners as $banner)<tr><td>{{ $banner->id_banner }}</td><td><img src="{{ asset('vs-cuidadora/assets/'.$banner->img_banner) }}" alt="{{ $banner->titulo_banner }}" style="width:100px;height:55px;object-fit:cover"></td><td>{{ $banner->titulo_banner }}</td><td><span class="badge text-bg-{{ $banner->status_banner==='ATIVO'?'success':'secondary' }}">{{ $banner->status_banner }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.banner.edit',$banner) }}">Editar</a> <form class="d-inline" method="POST" action="{{ route('admin.banner.status',$banner) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">Status</button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-4 text-muted">Nenhum banner cadastrado.</td></tr>@endforelse</tbody></table></div><div class="card-footer">Total de banners: <strong>{{ $banners->count() }}</strong></div></div>
